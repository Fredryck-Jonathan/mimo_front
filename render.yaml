# render.yaml pour React SPA
services:
  - type: web
    name: my-react-app
    branch: main
    buildCommand: npm install && npm run build
    staticPublishPath: build   # dossier créé par CRA
    plan: free                 # free fonctionne ici
